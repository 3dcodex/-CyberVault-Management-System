[build]
buildCommand = "python manage.py collectstatic --noinput"

[deploy]
startCommand = "python manage.py migrate && python manage.py createcachetable --no-color && gunicorn cybervault.wsgi:application --bind 0.0.0.0:$PORT --log-file=-"
